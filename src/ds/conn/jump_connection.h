#ifndef DGL_DS_COMM_JUMP_CONNECTION_H_
#define DGL_DS_COMM_JUMP_CONNECTION_H_

#include "./connection.h"

namespace dgl {
namespace ds {

/*
class JumpConnection: public Connection {
 public:
  void SendSetup(SendDevMem** send_dev_mem, void** send_resources,
                        int buffer_size, ConnInfo* conn_info,
                        ExchangeConnInfo* ex_info) = 0;

};
*/

}
}

#endif